// Re-export all place-domain types from the canonical types file
// for convenient feature-local imports

export type {
  Place,
  PlaceType,
  PlaceCategory,
  ContactMode,
  PlaceContact,
  PlaceLocation,
  PlaceResource,
  MediaReference,
  LocalizedText,
  LocalizedSeo,
  PublishStatus,
} from '@/types';
